[project]
name = "workflows"
version = "0.1.0"
description = "Render SDK Workflow Examples"
requires-python = ">=3.10,<4.0"
dependencies = [
    "render_sdk>=0.1",
    "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.poetry]
package-mode = false
